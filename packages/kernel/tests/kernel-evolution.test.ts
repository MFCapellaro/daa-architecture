import { describe, expect, test } from "vitest"; import { KernelRelationship, Verbs, Identity } from "../index.js"; describe("DAA Kernel evolution", () => { test("creates new relationships without changing kernel structure", () => { const continuity = { id: "continuity", name: "Continuity", definition: "The persistence of identity through transformation." }; const identityContinuity = KernelRelationship.of( Identity, Verbs.Preserves, continuity ); expect(identityContinuity.source) .toBe(Identity); expect(identityContinuity.verb) .toBe(Verbs.Preserves); expect(identityContinuity.target) .toBe(continuity); }); test("preserves relationship identity during evolution", () => { const continuity = { id: "continuity", name: "Continuity", definition: "The persistence of identity through transformation." }; const relationship = KernelRelationship.of( Identity, Verbs.Preserves, continuity ); expect(relationship.id) .toBe("identity:preserves:continuity"); }); });