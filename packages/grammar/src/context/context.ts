/**
 * Context
 *
 * Purpose
 * Context is a Grammar concept that establishes the semantic frame within DAA.
 *
 * Relationships
 * - Provides surrounding semantic conditions for architectural coherence.
 * - Exists within Grammar as part of the shared language of DAA.
 */
export interface Context {
  readonly kind: 'Context';
}