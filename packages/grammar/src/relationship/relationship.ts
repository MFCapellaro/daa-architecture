/**
 * Relationship
 *
 * Purpose
 * Relationship is a Grammar concept that establishes semantic coherence between elements within DAA.
 *
 * Relationships
 * - Enables coherent distinction and association between semantic elements.
 * - Exists within Grammar as part of the shared language of DAA.
 */
export interface Relationship {
  readonly kind: 'Relationship';
}