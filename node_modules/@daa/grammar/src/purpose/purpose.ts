/**
 * Purpose
 *
 * Purpose
 * Purpose is a Grammar concept that expresses reason for existence within DAA.
 *
 * Relationships
 * - Gives direction to semantic identity.
 * - Exists within Grammar as part of the shared language of DAA.
 */
export interface Purpose {
  readonly kind: 'Purpose';
}