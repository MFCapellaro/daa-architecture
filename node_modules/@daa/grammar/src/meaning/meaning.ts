/**
 * Meaning
 *
 * Purpose
 * Meaning is a Grammar concept that establishes shared significance within DAA.
 *
 * Relationships
 * - Gives architectural elements a common semantic basis.
 * - Derives its place from Grammar as the shared conceptual language of DAA.
 */
export interface Meaning {
  readonly kind: 'Meaning';
}